# Environment Setup

Choose the folder for the AI coding tool you have chosen and follow the homework instructions.

- **`claude-code/`** - Claude Code setup (CLAUDE.md template + skills)
- **`codex/`** - Codex setup (`AGENTS.md` template + skills)
- **`github-copilot/`** - GitHub Copilot setup (instructions + prompts)
- **`cursor/`** - Cursor setup (rules + skills)
- **`monorepo/`** - Automation for generating context files across monorepo directories
